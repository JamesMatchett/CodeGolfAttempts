static int x(int y)
        {
            var z = y.ToString();
            int r = z[0]-48;
            for (int i = 1; i < z.Length; i++)
            {
                if (z[i] != 48)
                {
                    r *= z[i]-48;
                }
            }
            return r;
        }
